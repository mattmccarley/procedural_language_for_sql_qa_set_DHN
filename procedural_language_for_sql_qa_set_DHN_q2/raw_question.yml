title: Q2
type: scored_question
prompt: What is a cursor in procedural languages for SQL, and what is its primary purpose in interacting with database tables?
fields:
checkbox_answers:
    type: checkbox
    max_score: 3
    label: Check if answered correctly
    checkboxes:
      - label: "Definition: A cursor is a database object that acts as a pointer to a result set of a SELECT statement, allowing row-by-row processing of the retrieved data."
        points: 1
        value: ans_0
      - label: "Primary purpose: Cursors enable procedural code to iterate through and manipulate individual rows returned by a query, facilitating operations that require row-level processing or complex data manipulations not easily achieved with set-based SQL operations."
        points: 1
        value: ans_1
